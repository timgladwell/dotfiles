set -g mouse on

# Remove the delay after pressing Esc (matters for vim/nvim)
set -sg escape-time 0

# Support for extended keys e.g. shift-enter
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'